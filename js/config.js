/* Site configuration.
   CARTO basemap key: free at https://carto.com/basemaps/apikey (emailed on
   request). It ships to every visitor, so restrict it to this site's domains
   in the CARTO key settings. Without a key the tiles carry an
   "API KEY REQUIRED" watermark; markers and timeline work either way. */
window.BUCEES_CONFIG = {
  cartoKey: ""
};
